import React, { useState } from "react";
import Layout from "../../../components/layout";

const BuildingOrBlocks = () => {
  return (
    <React.Fragment>
      <Layout>
        <h1>Admin BuildingOrBlocks</h1>
      </Layout>
    </React.Fragment>
  );
};

export default BuildingOrBlocks;
