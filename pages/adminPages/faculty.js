import React, { useState } from "react";
import Layout from "../../components/layout";

const Faculty = () => {
  return (
    <React.Fragment>
      <Layout>
        <h1>Admin Faculty</h1>
      </Layout>
    </React.Fragment>
  );
};

export default Faculty;
